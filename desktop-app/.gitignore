# Python
__pycache__/
*.pyc

# App output / downloaded media (user's actual downloads, not repo content)
Output/

# Runtime-generated by setup.bat (machine-specific resolved python path)
python_cmd.txt

# PyInstaller build artifacts
build/
dist/
*.spec

# Staged ffmpeg binaries pulled in by build_exe.bat
ffmpeg_bin/

# Inno Setup output
installer_output/

# App icons — regenerated from assets_data.py (base64), not stored as binaries
assets/YT-Downloader.ico
assets/YT-Downloader.png
