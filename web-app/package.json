{
  "name": "yt-downloader-web",
  "version": "1.0.0",
  "description": "Web port of YT Downloader - fetch music (MP3) or video (MP4) from a YouTube link or search terms. yt-dlp fetch proxy on the server; MP3 transcode (ffmpeg.wasm) and playlist zipping (JSZip) happen in the browser.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "postinstall": "node scripts/download-ytdlp.js && node scripts/setup-wasm.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@ffmpeg/core-mt": "0.12.10",
    "@ffmpeg/ffmpeg": "0.12.15",
    "express": "^4.19.2",
    "ffmpeg-static": "^5.2.0",
    "jszip": "3.10.1"
  }
}
